# Placeholder names only. Never put real secrets in this file.
# AI Council은 런타임 비밀정보(API 키, Telegram 토큰)를 .env가 아니라
# electron-store에 저장한다. 앱 실행에 필수 .env 값은 없다.
# EXAMPLE_API_KEY=
# EXAMPLE_BASE_URL=
