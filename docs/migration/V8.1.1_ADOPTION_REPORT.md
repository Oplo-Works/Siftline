# v8.1.1 Workflow Adoption Report

- Task: workflow-adoption-v8.1.1
- Date: 2026-07-11 (UTC)
- Repository: `C:\Users\parkm\Documents\AI-Council-Chat` (origin:
  `https://github.com/MinkyuTheBuilder/ai-council.git`)
- Executor: Claude Code, observed model `claude-fable-5` (PIN status: CANDIDATE)

## Executive Summary

AI Council 저장소를 레거시 "Project Engineering OS v5 Lean" 워크플로에서
**AI Coding Agent Workflow v8.1.1-solo**로 전환했다. 25파일 번들을 검증 후
workflow-owned 19파일을 설치했고, 레거시 활성 워크플로 파일 4종을 타임스탬프
아카이브에 보존한 뒤 bootstrap(CLAUDE.md/AGENTS.md)을 merge, 레거시
`docs/AGENT_WORKFLOW.md`를 SUPERSEDED pointer로 교체, PROJECT_SCOPE를 v8.1.1
schema로 재구성(모든 검증된 프로젝트 사실 보존), MODEL_RUNTIME_PIN·HANDOFF를
실제 관찰값 기반으로 신설했다. **애플리케이션 소스·테스트·의존성·schema·CI·배포
파일은 변경하지 않았다.** `npx tsc --noEmit`과 `npm run build` 모두 PASS.
PIN에 `APPROVED`된 Independent Reviewer Runtime이 없어 공식 독립 리뷰는 대기
상태이며, 최종 결과는 `READY_FOR_REVIEW`다 (push 보류).

## Detected Source Workflow

- **Classification: `LEGACY_V5`** (confidence: High)
- Evidence:
  - `docs/PROJECT_STATE.md`: "v5 Lean Field Test용", "System under test: Project
    Engineering OS v5 Lean" 명시
  - `docs/AGENT_WORKFLOW.md`(구): 레거시 stage 라벨
    `/blueprint → /spec → /plan → /build → /test → /review → /log`
  - 단일 monolithic `docs/AGENT_WORKFLOW.md`가 활성 워크플로 원본으로 사용됨
  - 구 CLAUDE.md의 "항상 main 브랜치에서 커밋" per-session Git 규칙
  - `docs/PROJECT_ENGINEERING_OS_FIELD_TEST_LOG.md`: v5 Lean 필드테스트 3건 기록
  - 외부 매뉴얼 폴더에 `AI_Coding_Agent_Workflow_v5.md` 실존 (참고 정황)

## Pre-Adoption Repository State

- Branch: `main` @ `68a2700b03d75b325a52f8fc7665ad1c07325b02` (= tag `v1.0.9`,
  origin/main과 동기화 확인)
- Worktree classification: **USER_DIRTY_ONLY** — dirty: `package-lock.json`
  (수정, unstaged, 사용자 소유). staged 변경: 없음.
- 다수의 오래된 `.claude/worktrees` worktree(prunable, 타 사용자 경로) 존재 —
  건드리지 않음.
- CI: `.github/workflows/build.yml` — `v*` 태그 push 및 workflow_dispatch에서만
  트리거. **브랜치 push는 CI/배포를 트리거하지 않음** (확인됨).
- Baseline checks (도입 전 상태 기준, 앱 소스 무변경이므로 도입 후 실행 결과와 동일):
  `npx tsc --noEmit` PASS, `npm run build` PASS.
- Pre-adoption SHA256 (변환 대상 파일):
  - `CLAUDE.md` `fc06e2bed5a998f540394e9ea6ef6d2d11ad2c886145456ff353eb2f50c02922`
  - `docs/AGENT_WORKFLOW.md` `815c63a46172621c5cf86b8d39d9f479ed46cc63dfa4a9b38ed8fe8957109573`
  - `docs/PROJECT_SCOPE.md` `65d65f07523a5b5e6f1b06319fc42230b7e0145cf5c8c1a145da897b1e73ea78`
  - `docs/DEV_LOG.md` `1528b7c9ded1cbb990c9027501b6c91b0f3bd027463047c44d914fe3b2b9e909`
  - `.gitignore` `7cfeb87b8b4a1e2e9086d67a9293ea73ddeccc3e70b3542d2dfb7bb66f4488c3`
  - 기타 보존 문서 해시는 adoption 커밋 이전 Git history로 확인 가능

## Package Verification

- ZIP: `AI_Coding_Agent_Workflow_v8.1.1_Solo_files.zip` — SHA256
  `6867F85F1D32FC9AF9CF7659FA16D7395B48AE78C1B60AF673BB26B3BD6045D6`
- Master MD: `AI_Coding_Agent_Workflow_v8.1.1_Solo.md` — SHA256
  `6FFA19AD8D267DB4C5112298BC986C045ED05666EB90B21E85E3C0CEFCE58E77`
- 임시 디렉토리(scratchpad)에 추출. 레포에 직접 unzip하지 않음.
- 파일 수: **25/25 확인** (기대 번들과 일치)
- 경로 이탈/절대경로/심볼릭 링크/실행 파일: **없음** (전부 `.md` + 텍스트 template 2종)
- ZIP ↔ master FILE 블록 비교: **25/25 내용 일치** (개행 정규화 기준, 스크립트 검증)

### Target Classification

| Target | Classification |
|---|---|
| `WORKFLOW_INSTALL.md`, `docs/AGENT_WORKFLOW_CORE.md`, `docs/workflow/*` (9), `docs/MODEL_RUNTIME_SNAPSHOT.md`, `docs/{features,product,architecture,design,handoff_history}/README.md`, `templates/*` (2) | NEW_SAFE — 기존 파일 없음, ZIP 원본 설치 |
| `CLAUDE.md` | PROJECT_OWNED_MERGE (레거시 워크플로 블록 교체 + 프로젝트 고유 규칙 보존) — 원본 아카이브 |
| `AGENTS.md` | NEW_SAFE (merge 형태로 신설) |
| `docs/AGENT_WORKFLOW.md` | WORKFLOW_OWNED_REPLACE_AFTER_ARCHIVE → SUPERSEDED pointer |
| `docs/PROJECT_SCOPE.md` | PROJECT_OWNED_MERGE (v8.1.1 schema 채택, 사실 보존) — 원본 아카이브 |
| `docs/MODEL_RUNTIME_PIN.md`, `docs/HANDOFF.md` | NEW_SAFE (실제 상태 기반 재구성; template 복사 아님) |
| `docs/DEV_LOG.md` | HISTORICAL_PRESERVE + append-only 도입 이벤트 1건 |
| `.gitignore` | PROJECT_OWNED_MERGE (additive snippet만 추가) |
| `.env.example` | NEW_SAFE (placeholder-only 신설) |
| `README.md` | NOT_APPLICABLE — 워크플로 링크 없음 확인, 무변경 |
| `docs/PROJECT_STATE.md`, `docs/VERIFICATION.md`, `docs/PRODUCT_BLUEPRINT.md`, `docs/PROJECT_ENGINEERING_OS_FIELD_TEST_LOG.md`, `docs/plans/*`, `docs/specs/*` | HISTORICAL_PRESERVE — 무변경 |
| `package-lock.json` | USER_DIRTY — 무변경 보존 |

## Archived Legacy Files

- Archive: `docs/archive/workflow/pre-v8.1.1-20260711T124715Z/`
- 내용: `CLAUDE.md`, `docs/AGENT_WORKFLOW.md`, `docs/PROJECT_SCOPE.md`,
  `docs/DEV_LOG.md`(append 전 스냅숏) + `MANIFEST.md`(SHA256·사유 포함)
- 애플리케이션 소스는 아카이브하지 않음. Git history가 1차 이력.

## Installed Workflow-Owned Files

19파일 설치, 설치 후 SHA256이 ZIP 원본과 **전부 일치** 확인:
`WORKFLOW_INSTALL.md`, `docs/AGENT_WORKFLOW_CORE.md`,
`docs/workflow/{BUILD,CLOSE,GIT_SAFETY,MICRO,PLAN,PRODUCT,REVIEW,SPEC,TEST}.md`,
`docs/MODEL_RUNTIME_SNAPSHOT.md`,
`docs/{features,product,architecture,design,handoff_history}/README.md`,
`templates/gitignore.workflow-snippet`, `templates/env.example.workflow-template`

## Project-Owned Merge Decisions

- **CLAUDE.md / AGENTS.md**: v8.1.1 bootstrap 블록(읽기 순서 CORE → PIN → SCOPE →
  HANDOFF → stage map → GIT_SAFETY → SPEC/PLAN)을 활성 블록으로 사용. 프로젝트
  고유 기술 규칙(빌드 명령, selectors.json, electron-store, Edit-truncate 주의,
  Later Phase 금지, worktree 경로 주의)은 "Project-Specific Instructions" 섹션에
  보존. 레거시 "항상 main 브랜치 커밋" Git 규칙은 보존하지 않고 HUMAN-OWNED
  정책으로 대체 (사용자 재변경 옵션을 SCOPE §7에 명시).
- **레거시 경로 사실 갱신**: 구 문서의 레포 경로 `C:\Users\Sales01\...`는 실제와
  불일치(현 `C:\Users\parkm\...`) — bootstrap에 현재 경로 반영.
- **.gitignore**: snippet 항목 중 미존재분(`.env.*`, `!.env.example`, `*.pem`,
  `*.key`, `secrets/`, `.local/`)만 additive 추가. 기존 규칙 무변경.
  `.env.example`이 ignore되지 않음을 `git check-ignore`로 확인.
- **.env.example**: 없어서 placeholder-only로 신설 (실값 없음).
- **README.md**: 워크플로 문서 링크가 없음을 확인(모든 "Workflow" 언급은 앱 기능)
  — 무변경.
- **중첩 bootstrap**: 레포 내 중첩 CLAUDE.md/AGENTS.md는 gitignore된 오래된
  `.claude/worktrees` 복사본뿐 — 무변경.

## Project Scope Reconciliation

- v8.1.1 schema 채택. 보존된 사실: 제품 목표, Must-preserve flows 전체(v1.0.9
  기능 셋), Out of Scope(Later Phase) 목록, 사용자/역할, 데이터 보안 규칙
  (electron-store, Telegram Chat ID reject, persist: 파티션), 검증 명령
  (tsc/build/build-and-run.bat + sandbox 제약).
- HUMAN-OWNED 정책은 이 adoption 프롬프트가 명시 승인한 값 그대로 기록
  (AUTO_LOCAL_AFTER_TEST / AUTO_AT_CLOSE / protected `main,master,production,release*`
  / merge·rebase ASK_ALWAYS / 유료·외부·배포 ASK 계열).
- 추가 안전장치: `v*` 태그 push = 릴리스 CI 트리거 사실을 ASK_SEPARATELY로 명문화.
- **Status: `READY_FOR_APPROVAL`** — 미확정: repo classification(Public 추정),
  training/retention, Runtime 승인, 유료 credit cap. (§7에 정확히 나열)

## Runtime Pin Reconciliation

- v8.1.1 구조로 신설. **관찰된 사실만 기록**: 이 세션의 runner=Claude Code,
  observed model `claude-fable-5`(2026-07-11), permission workspace-write.
  runner 버전·구독 tier·billing meter 미관찰로 미기재.
- Codex/z.ai 접근성 미확인 — 전 항목 template 값 그대로 `CANDIDATE`.
- 발명된 관찰값·예산·승인: **없음**. Status: `DRAFT`.
- SCOPE의 Approved Runtime IDs: 비어 있음 (APPROVED PIN 항목이 없으므로 규칙 준수).

## Completed Work Reconciliation

- `docs/migration/LEGACY_WORK_INDEX.md` 작성 — 의미 있는 10개 항목
  (릴리스 마일스톤, v5 도입, Field Test 3건, 개별 수정 2건, v1.0.9 레이아웃/릴리스,
  in-flight 사용자 변경).
- 분류 근거: git tags/커밋 실존, DEV_LOG의 검증 기록(tsc/build PASS, Windows 확인),
  `git ls-remote` 원격 태그 확인. 증거 없는 항목은 `COMPLETED_BUT_UNVERIFIED`로
  정직 기록. 소급 SPEC/PLAN/승인/증거 생성 없음.

## Active Work Reconciliation

- 미완료 워크플로 task: 없음 (마지막 작업 v1.0.9 릴리스는 커밋·태그 완료).
- v1.0.9 릴리스 CI 산출물(installer)은 로컬에서 검증 불가 →
  `COMPLETED_BUT_UNVERIFIED`, 사용자 확인 권장.
- `package-lock.json` 미커밋 수정: 사용자 소유 in-flight 변경으로 보존
  (stage/commit/폐기하지 않음). HANDOFF에 기록.
- 구 `PROJECT_STATE.md`의 2026-05-26 "In flight" 항목들은 이후 커밋에 포함된 것으로
  확인됨 (해당 파일들은 현재 clean).

## Future-Work Enforcement

| Check | Result |
|---|---|
| 미래 세션 읽기 순서 = CORE → PIN → SCOPE → HANDOFF → stage map → GIT_SAFETY → SPEC/PLAN | PASS (CLAUDE.md/AGENTS.md bootstrap) |
| v8.1.1이 유일한 활성 워크플로 권위 | PASS |
| `docs/AGENT_WORKFLOW.md` = SUPERSEDED pointer만 | PASS |
| v5/v7/레거시를 가리키는 활성 bootstrap 없음 | PASS (레거시 언급은 pointer 출처 표기·아카이브 참조·HANDOFF 이력 서술뿐) |
| 매 세션 구 전체 매뉴얼 읽기 지시 없음 | PASS |
| literal `<CURRENT-STAGE>.md` 없음 | PASS |
| `FIX_REQUESTED` review mode 없음 | PASS |
| Reviewer enum에 `APPROVED_WITH_RISK` 없음 (Human Decision 전용) | PASS |
| High PASS가 Human Decision을 건너뛸 수 없음 | PASS (CORE §4/§9, REVIEW routing) |
| BUILD/TEST/REVIEW에서 Standard/High push 금지 | PASS (CORE §5, GIT_SAFETY §4) |
| HANDOFF가 권한을 부여하지 않음 | PASS (bootstrap 명시) |
| 실제 모델 routing은 PIN에만 존재 | PASS (CORE/playbook에 모델명 없음 — grep 확인) |
| 역사적 작업에 증거 조작 요구하는 활성 파일 없음 | PASS |

## Policy Applied

- Local Commit: `AUTO_LOCAL_AFTER_TEST` (task-owned 경로만, 명시적 path staging)
- Push: `AUTO_AT_CLOSE` → `origin/chore/adopt-workflow-v8.1.1` 한정.
  protected(`main` 등) 직접 auto-push: NEVER. 독립 리뷰 PASS 전 push 보류.
- PR 생성: 하지 않음 (프롬프트 지시)
- 사용자 변경(`package-lock.json`): 무변경 보존, stage 안 함, `git add .` 미사용

## Validation

| Check | Command / Method | Result |
|---|---|---|
| 번들 무결성 | ZIP 추출 + 파일 수 + 경로 검사 | PASS (25/25, 이탈 없음) |
| ZIP↔master 일치 | FILE 블록 파싱·정규화 비교 스크립트 | PASS (25/25) |
| 설치 파일 무결성 | 설치본 vs ZIP SHA256 비교 | PASS (19/19 일치) |
| Markdown/참조 | 활성 내부 경로 실존 검사 | PASS |
| Stage map 해석 | CORE stage map ↔ `docs/workflow/*.md` 9종 실존 | PASS |
| 레거시 회귀 탐색 | grep (CURRENT-STAGE, FIX_REQUESTED, v5/v7 선언, CORE/playbook 모델명) | PASS (허용된 참조 3건만 — pointer/아카이브/이력 서술) |
| Typecheck | `npx tsc --noEmit` (repo root) | **PASS** (exit 0) |
| Build | `npm run build` (repo root, Windows) | **PASS** (exit 0; renderer+electron 번들 생성) |
| 수동 앱 체크리스트 | `build-and-run.bat` | NOT_RUN — 앱 소스 무변경이므로 불필요; 문서만 변경 |
| Whitespace/conflict marker | `git diff --check` (task 파일) | PASS (CRLF 정규화 경고만 — 레포 기존 설정) |
| Secret scan | 신규/변경 task 파일 패턴 grep + 수동 검토 | PASS (secret/PII 없음) |
| 앱 소스 무변경 | `git status` 소유권 검사 | PASS (task-owned 경로 + 사용자 `package-lock.json`만) |
| `git add .` 미사용 | 절차 준수 | PASS |

Baseline 실패: 없음 (기존 빌드·타입체크 모두 통과 상태였음).

## Independent Review

- PIN에 `APPROVED` 상태의 Independent Reviewer Runtime이 존재하지 않음
  (모든 Runtime이 CANDIDATE — 사용자 승인 전).
- 조치: 같은 모델(`claude-fable-5`)의 **fresh-context 자문(advisory) 리뷰**를
  ARTIFACT_READ_ONLY 모드로 수행하고 `docs/migration/V8.1.1_ADOPTION_REVIEW.md`에
  기록. 이는 self-review 대체 금지 규칙에 따라 **공식 독립 리뷰로 인정하지 않음**.
- 공식 독립 리뷰 실행 방법: 아래 "Remaining Human Inputs" 및 REVIEW 파일의
  리뷰어 프롬프트 참조. Review Range: `68a2700..<adoption artifact commit>`.

## Git and Publish

- Branch: `chore/adopt-workflow-v8.1.1` (main에서 분기, base `68a2700`)
- Commits: (1) adoption artifact commit — 이 리포트 포함 (SELF),
  (2) advisory review artifact metadata commit (리뷰 후)
- Push: **NOT_ATTEMPTED** — 독립 리뷰 게이트 미충족으로 보류.
  승인 시 target: `origin/chore/adopt-workflow-v8.1.1` (한 번).
  브랜치 push는 CI를 트리거하지 않음 (build.yml은 태그/dispatch 전용 — 확인됨).
- `main` merge: ASK_ALWAYS — 사용자 결정.

## Remaining Human Inputs

1. **독립 리뷰 또는 직접 승인**: (a) 승인된 다른 runtime(예: Codex read-only)으로
   `docs/migration/V8.1.1_ADOPTION_REVIEW.md`의 리뷰어 프롬프트를 실행하거나,
   (b) 사용자가 adoption diff를 직접 검토·승인. 이후 WF:CLOSE에서
   `origin/chore/adopt-workflow-v8.1.1`로 push.
2. `docs/PROJECT_SCOPE.md` §7 미확정 항목 확정 후 Status `APPROVED`로 변경.
3. `docs/MODEL_RUNTIME_PIN.md` 관찰값 채우고 사용할 Runtime을 `APPROVED`로 전환,
   해당 ID를 SCOPE §5에 복사.
4. 레거시 "항상 main 커밋" 습관 유지 여부 결정 (기본: task branch + CLOSE push).
5. `main`으로의 merge 실행 여부 (merge는 ASK_ALWAYS).
6. v1.0.9 GitHub Release 산출물 확인 (LEGACY_WORK_INDEX follow-up).
7. `package-lock.json` 미커밋 변경 처리 (커밋 또는 폐기).

## Final Outcome

**READY_FOR_REVIEW**
